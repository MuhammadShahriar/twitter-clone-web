// Placeholder "T" brand mark — the 44px accent circle shown at the top of the
// auth form (the locked brand mark / real logo arrives in a later module).
export function Logo() {
  return (
    <div className="grid h-11 w-11 place-items-center rounded-full bg-accent text-2xl font-extrabold tracking-[-0.02em] text-white">
      T
    </div>
  );
}
