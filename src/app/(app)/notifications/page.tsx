import { Notifications } from "@/components/Notifications";

// /notifications — the notifications list inside the shared app shell (Module 5C).
export default function NotificationsPage() {
  return <Notifications />;
}
