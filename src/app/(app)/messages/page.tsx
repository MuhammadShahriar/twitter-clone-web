import { Conversations } from "@/components/Conversations";

// /messages — the DM conversation list inside the shared app shell (Module 12).
export default function MessagesPage() {
  return <Conversations />;
}
